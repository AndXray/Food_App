<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.diplom2">

    <application

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Diplom2.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name=".screens.physic_data.NewPhysicData"
            android:exported="false"
            android:label="@string/title_activity_new_physic_data"
            android:theme="@style/Theme.Diplom2.NoActionBar" />
        <!-- <activity -->
        <!-- android:name=".Glavnoe" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".King" -->
        <!-- android:exported="false" -->
        <!-- android:label="@string/title_activity_king" -->
        <!-- android:theme="@style/Theme.Diplom2.NoActionBar" /> -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>


    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


</manifest>